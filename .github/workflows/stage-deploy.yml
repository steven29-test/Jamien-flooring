name: Stage Deployment

on:
  push:
    branches:
      - Stage

jobs:
  stage-build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: stage
      url: https://steven29-test.github.io/Jamien-flooring/

    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build the app
        run: npm run build

      - name: Remove CNAME for stage (no custom domain)
        run: rm -f dist/CNAME

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2

      - name: Stage Deployment Summary
        run: |
          echo "✅ Stage Deployment Completed!"
          echo "URL: https://steven29-test.github.io/Jamien-flooring/"
          echo "Commit: ${{ github.sha }}"
